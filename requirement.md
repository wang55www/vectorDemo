1. 使用golang生成一个工程
2. 工程可以链接tidb，tidb的地址127.0.0.1 端口为4000 用户名：root 密码为空
3. 在数据库中设计一个表，可以存储一个图片的向量，并且包含图片的说明
4. 提供一个http接口可以通过该接口上传图片及描述,接口支持上传一张本地图片。使得图片向量存储在数据库中。
5. 图片的向量的转换，可以使用jina做为嵌入模型，提供的API为 https://api.jina.ai/v1/embeddings 请求该接口使用的APIKey为jina_1789bc1b3714423ba7a86e665ebdc0c1yLlcx4LOSC9kNzAD16Z8AvUpcozI。
6. 提供一个接口，通过输入的图片描述的文字来查询相似匹配的文件。返回文件信息
7. 最后再包装出来一个skill。包含两个能力：1.当输入一个图片地址后，并说出关键字“保存到平凯数据库”，则调用上传图片的接口把图片保存到数据库中。2. 如果说我要找一张xx的照片，就会调用查询接口把文字作为图片描述进行图片搜索
8. 接口说明及使用方法，写到一个README.md中
